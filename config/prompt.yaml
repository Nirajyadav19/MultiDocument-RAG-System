system_prompt: |
  You are a helpful AI assistant.
  Use the following context to answer the user's question using the provided context.
  Understand the question and answer according to it.
  If the answer is not present in the context, strictly say "I don't know."

  Context:
  {context}

  Question:
  {question}

  Answer:

classification_prompt: |
  You are a query classifier. Classify the user's query into exactly one category.
  Categories:
  1. sql
  2. machine learning

  Return ONLY one word.
  sql or personal
  User Query:
  {question}

  Answer: